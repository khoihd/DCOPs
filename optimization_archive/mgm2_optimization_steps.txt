# MGM2 Optimization Steps

## Status

Complete. No further MGM2 optimization is planned in this pass.

## Baseline

Initial focused checks:

- `conda run -n khoihd python -m pytest tests/unit/test_algorithms_mgm2.py`
- `conda run -n khoihd ruff check pydcop/algorithms/mgm2.py tests/unit/test_algorithms_mgm2.py`

Both passed before edits.

## Step 1: Memory Accounting

Completed:

- Fixed `computation_memory()` to exclude the computation variable by exact
  variable-name equality, instead of substring membership.
- Added regression coverage for similarly-prefixed variable names such as
  `v1` and `v10`.

Verification:

- `conda run -n khoihd python -m pytest tests/unit/test_algorithms_mgm2.py`
- `conda run -n khoihd ruff check pydcop/algorithms/mgm2.py tests/unit/test_algorithms_mgm2.py`
- `conda run -n khoihd python -m pytest tests/api/test_api_solve.py tests/api/test_api_graph.py -k mgm2`

## Step 2: Profile Local Evaluation

Completed:

- Added `profiling/profile_mgm2_local_eval.py`.
- The profiler builds MGM2 computations directly and times
  `_compute_best_value()` and `_compute_offers_to_send()`.
- It records aggregate timings for wrapped helper calls:
  `assignment_cost()` and `_compute_cost()`.
- It supports production-like cache behavior by default, plus
  `--clear-cache-per-repeat` for cold-cache measurements.
- It also supports `--cprofile` for cumulative stack timings.

Sample runs:

- `conda run -n khoihd python profiling/profile_mgm2_local_eval.py`
- `conda run -n khoihd python profiling/profile_mgm2_local_eval.py --operation both --case generated --repeat 200 --neighbors 8 --constraints 24 --domain-size 8 --max-arity 4 --cprofile`
- `conda run -n khoihd python profiling/profile_mgm2_local_eval.py --operation both --case graph-coloring --repeat 200`
- `conda run -n khoihd python profiling/profile_mgm2_local_eval.py --operation both --case generated --repeat 50 --neighbors 8 --constraints 24 --domain-size 8 --max-arity 4 --clear-cache-per-repeat`

Observed:

- `_compute_offers_to_send()` dominates the combined local-evaluation profile.
- Production `_compute_cost()` caching greatly reduces repeated
  `assignment_cost()` calls across repeated candidate assignments.
- With a cold cache, `assignment_cost()` dominates local evaluation time.
- `assignment_cost()` remains out of scope for this MGM2 pass based on its
  relation docstring.

Verification:

- `conda run -n khoihd ruff check profiling/profile_mgm2_local_eval.py`

## Step 3: Direct Offer Candidate Iteration

Completed:

- Replaced `generate_assignment_as_dict([self.variable, self._partner])` in
  `_compute_offers_to_send()` with direct nested iteration over the variable
  and partner domains.
- Cached local references for `current_cost`, `mode`, `self.name`, and
  `partner.name`.
- Preserved the call to `_compute_cost(**partial_asgt)` so existing
  `lru_cache` behavior remains intact.
- Updated `profiling/profile_mgm2_local_eval.py` because offer generation no
  longer calls `generate_assignment_as_dict()`.

Observed:

- Production-cache generated sample remained behavior-compatible and slightly
  faster in the sampled run.
- Cold-cache profile still shows `assignment_cost()` as the dominant cost,
  which remains out of scope.

Verification:

- `conda run -n khoihd python -m pytest tests/unit/test_algorithms_mgm2.py`
- `conda run -n khoihd ruff check pydcop/algorithms/mgm2.py tests/unit/test_algorithms_mgm2.py profiling/profile_mgm2_local_eval.py`
- `conda run -n khoihd python -m pytest tests/api/test_api_solve.py tests/api/test_api_graph.py -k mgm2`

Closure:

- `assignment_cost()` will not be optimized as part of this MGM2 pass. Its
  docstring documents why it remains a generic, behavior-sensitive relation
  helper.
- If MGM2 is revisited later, start a new focused plan and preserve
  `_compute_cost()` cache behavior.
