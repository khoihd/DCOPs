[metadata]
# This includes the license file in the wheel.
license_file = LICENSE

[tool:pytest]
addopts = -m "not perf"
markers =
    perf: performance and benchmark tests excluded from default correctness runs
